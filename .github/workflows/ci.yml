name: CI

on:
  push:
    paths:
      - .github/workflows/ci.yml
      - arealite/**
  workflow_dispatch:

jobs:
  test-arealite:
    runs-on: ubuntu-latest
    steps:
      - uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.CI_NODE_ADDR }}
          key: ${{ secrets.REMOTE_SSH_KEY }}
          script_path: scripts/run_test_arealite.sh
