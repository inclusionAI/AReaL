# Code for test
## Run
```bash
./test_32_times_auto.sh -m /your/path/to/model
```

It will dump all the evaluation result to a directory in the model's directory

## Report
```bash
python3 aggregate_accuracy.py --test-dir OUTPUT_BASE_DIR
```
