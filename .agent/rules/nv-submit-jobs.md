---
trigger: always_on
---

for running `python` `bash` related scripts, run in the following format

```
eai-run -i --pty <your command> 
```

for example, `nvidias-smi` will be 

`eai-run -i --pty nvidia-sm
